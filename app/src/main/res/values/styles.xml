<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorControlNormal">#c5c5c5</item>
      <!--  <item name="colorControlActivated">@color/EventsGreen</item>
        <item name="colorControlHighlight">@color/EventsGreen</item>-->
    </style>

    <!-- Change the theme of the Dialog in ParentsHome -->
    <style name="AlertDialogTheme" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:windowTitleStyle">@style/MyTitleTextStyle</item>
        <!-- Used for the buttons -->
        <item name="colorAccent">#0e465d</item>
        <item name="android:textColor">#000000</item>
        <!-- Used for the text -->
        <item name="android:textColorPrimary">#000000</item>
        <!-- Used for the background -->
        <item name="android:background">#d1eefa</item>
    </style>
    <!-- Used for the title -->
    <style name="MyTitleTextStyle">
        <item name="android:textColor">#000000</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Title</item>
    </style>

    <!-- Used for the dialogs in time picker, date picker etc. -->
    <style name="PickDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/EventsGreen</item>
    </style>


</resources>
